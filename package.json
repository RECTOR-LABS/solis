{
  "name": "solis",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:run": "pnpm -r test:run",
    "lint": "pnpm -r lint",
    "dev": "pnpm --filter @solis/web dev",
    "agent": "pnpm --filter @solis/agent start",
    "heartbeat": "pnpm --filter @solis/agent heartbeat",
    "deploy:agent": "npx esbuild packages/agent/src/heartbeat.ts --bundle --platform=node --format=esm --outfile=/tmp/solis-agent.mjs --external:pino-pretty --target=node22 --banner:js='import { createRequire } from \"module\"; const require = createRequire(import.meta.url);' && scp /tmp/solis-agent.mjs solis:~/solis/"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.6.2"
}
